export const selectExpenses = (state) => state.expenses.expenses;
export const selectSelectedExpense = (state) => state.expenses.selectedExpense;
export const selectExpensesLoading = (state) => state.expenses.loading;
export const selectExpensesError = (state) => state.expenses.error;
export const selectExpenseTypes = (state) => state.expenses.types;
